from setuptools import setup, find_packages

setup(
    name="flaskify",
    version="1.0.0",
    packages=find_packages(),
    author="Don Artkins",
    description="Flask-based web framework with version management",
)